## General

auto-update-channel = tip

env = XDG_CONFIG_HOME=/Users/philippucke/.config
command = /opt/homebrew/bin/nu

## Theme & Styling

theme = Kanagawa Wave

background-opacity = 0.8
background-blur-radius = 20 

mouse-hide-while-typing = true
window-decoration = true
macos-option-as-alt = true


## Shaders
## https://github.com/m-ahdal/ghostty-shaders
## Currently, shaders have to be placed next to the 
## config, due to a bug in the path resolution.
## Enabling a custom shader does only work when background-opacity is not set.

# custom-shader = inside-the-matrix.glsl
# custom-shader-animation = true

## Keybindings

keybind = global:cmd+/=toggle_quick_terminal
